# Decisions

## 2026-08-03 — [Codex] Authoritative metadata icon set
- Use versioned public TrainerSim assets through Next metadata and keep `src/app/favicon.ico` absent.
- This prevents Next App Router file conventions from silently adding a later icon link that overrides product branding.
